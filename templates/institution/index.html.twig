{% extends 'template.html.twig' %}
{% block headTile %}

    <ol class="breadcrumb">
        <li>
            <a href="{{path('add-institution')}}" type="button" class="btn btn-block btn-success"> Ajouter Institution</a>
        </li>

    </ol>
{% endblock %}
{% block body %}
    <div class="box-body">
        <table id="users" class="table table-striped table-bordered dt-responsive nowrap dataTable no-footer cell-border">
            <thead>
                <tr>
                    
                    <th>Titre</th>
                    <th>Numéro</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                {% for institution in institutions %}
                    <tr>
                       
                        <td>{{institution.title}}</td>
                        <td>{{institution.phone}}</td>
                      
                        <td>
                            <a href="{{path('edit-institution',{'id':institution.id})}}" class="btn btn-social-icon btn-dropbox"><i class="fa fa-edit"></i></a>
                            <a href="{{path('delete-institution',{'id':institution.id})}}" class="btn btn-social-icon btn-dropbox"><i class="fa fa-times"></i></a>
                        </td>
                    </tr>
                {%endfor%}
            </tbody>

        </table>
    </div>
{% endblock %}