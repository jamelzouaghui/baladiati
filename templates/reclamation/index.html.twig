{% extends 'template.html.twig' %}
{% block headTile %}

    <ol class="breadcrumb">
        <li>

        </li>

    </ol>
{% endblock %}
{% block body %}
    <div class="box-body">
        <div class="row">
            <div id="map-canvas" style="height:500px; width:600px">></div>
        </div>
        <table id="users" class="table table-striped table-bordered dt-responsive nowrap dataTable no-footer cell-border">
            <thead>
                <tr>
                    <th>photo</th>
                    <th>Titre Réclamation</th>
                    <th>Date de Réclamation</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                {#    {% for institution in institutions %}
                        <tr>
                           
                            <td>{{institution.title}}</td>
                            <td>{{institution.phone}}</td>
                          
                            <td>
                             
                                <a href="#" data-toggle="modal" data-target="#modal-institution-edit-{{institution.id}}" class="btn btn-social-icon btn-dropbox" ><i class="fa fa-edit"></i></a>
                                <a href="{{path('delete-institution',{'id':institution.id})}}" class="btn btn-social-icon btn-dropbox"><i class="fa fa-times"></i></a>
                                   {{ render(controller( 'App\\Controller\\InstitutionController::editInstitution',{'id':institution.id} )) }}
                            </td>
                        </tr>
                    {%endfor%}#}
            </tbody>

        </table>
    </div>
    <script type="text/javascript" src="http://maps.googleapis.com/maps/api/js?sensor=false"></script>
    <script>
        var lat = 46.606111;
        var lng = 1.875278;
        var homeLating = new google.maps.LatLng(lat, lng);
        var inflowindow;
        var marker;
        var map;
        function initialize() {
            var myOptions = {
                center: homeLating,
                zoom: 6,
                mapTypeId: google.maps.MapTypeId.ROADMAP
            };
            map = new google.maps.Map(document.getElementById('map-canvas'), myOptions);
        }
        google.maps.event.addDomListener(window, 'load', initialize);
    </script>
{% endblock %}